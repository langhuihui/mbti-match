# 2026-07-31 工作日志

## admin 后台「云函数未部署或不可用」排查与修复

**问题**：admin 后台 Dashboard 顶部提示"云函数未部署或不可用"，降级显示 mock 数据。

**排查纠错**：第一轮用 Glob `cloudfunctions/*` 只返回 .DS_Store，误判"云函数代码缺失"。实际是 Glob 默认只列文件不列目录，子目录被漏看。用 `ls` 复核后发现 cloudfunctions 下有 4 个云函数：admin-api / track / msgSecCheck / llm-chat，admin-api 代码完整（index.js+handlers.js+auth.js+config.js+package.json+DEPLOY.md）。教训：核查目录结构别只靠 Glob 顶层匹配。

**根因**（两层，代码层已修 + 部署层待用户操作）：
1. [代码] admin 前端 `admin/src/services/cloudbase.js` 缺 CloudBase 登录——@cloudbase/js-sdk 在 Web 端 callFunction 前必须先 `auth.signInAnonymously()`，原代码只 `cloudbase.init()` 就直接调用，必然被云开发拒绝。
2. [部署] admin-api 云函数很可能未上传部署——文件为 7/30 新建，PROGRESS.md 部署记录只提 track/msgSecCheck，无 admin-api。

**已修复**（admin/src/services/cloudbase.js）：
- `initCloudBase` 改为 async，补 `app.auth({persistence:'local'}).signInAnonymously()`，附前置条件说明（需控制台开启匿名登录）。
- `verifyCloud` 增强错误日志：按报错文本区分「未部署(FUNCTION_NOT_FOUND) / 未登录(SIGN/UNAUTHORIZED) / 集合不存在(collection)」，浏览器 console 直接给处置建议；并补 `res.success===false` 分支的日志。

**待用户操作**（AI 无法代劳）：
1. 微信开发者工具：右键 cloudfunctions/admin-api → 上传并部署：云端安装依赖
2. 云开发控制台 → 设置 → 登录方式 → 开启「匿名登录」
3. 云开发控制台 → 数据库：建 users/assessments/assessments_sessions/relation_reports/learning_content/counselors/risk_alerts
4. admin-api 环境变量 ADMIN_TOKEN=dexter（默认值已与 admin/.env 一致，可不动）
5. 刷新 http://localhost:5174，看 Dashboard 顶部是否变绿 + console 报错

**遗留**：admin-api handlers 依赖 users/assessments 等业务集合，但小程序端 config/cloud.js 的 CLOUD_TRACKING_ENABLED=false，业务数据基本未上云。即使云函数跑通，明细数据可能仍空——属另一层问题，需后续单独处理。
